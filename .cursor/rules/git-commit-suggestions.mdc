---
description: After each code change, commit with a short commit message
alwaysApply: true
---

# Commit after each code change

After each code change, **commit it** in the same turn. Do not wait for the user to ask. Do not leave work uncommitted unless the user says not to commit, or the turn was question-only / read-only.

## Commit

- One short message (about one line). Match recent `git log` tone. Say why, not a file list.
- Run the commit in the same turn. Do not only suggest the command.
- `git add` explicit paths. Do not `git add -A` unless every change belongs in that commit.
- Pass the message with a HEREDOC. Do not skip hooks. Do not push unless asked.
- Never update git config. No force-push. No amend unless the user asked, HEAD is yours and unpushed, and a hook auto-modified files.

## Skip

- Questions, log inspection, or read-only review
- User says not to commit
- Nothing to commit
