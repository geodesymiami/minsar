# Plan: OPERA-like MiaplPy unwrap tiling (template + resize)

## Summary

Bake OPERA-like SNAPHU tiling into `EtnaSenA44.template` and teach `resize_miaplpy_unwrap_jobfiles.py` to size Launcher jobs when `--num_tiles > 1`.

## Status

Approved and implemented.
